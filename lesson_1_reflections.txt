Isolated differences between the versions
Could ignore the other untouched parts of program

Can see if the old parts of program worked and refer
back to it

Git clone will clone the repository. Git log will show the changes
made.  Git diff will compare two files and show a list of the changes

